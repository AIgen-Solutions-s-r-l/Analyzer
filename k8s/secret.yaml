apiVersion: v1
kind: Secret
metadata:
  name: analyzercore-secrets
  namespace: analyzercore
  labels:
    app.kubernetes.io/name: analyzercore
    app.kubernetes.io/component: api
type: Opaque
stringData:
  # IMPORTANT: Replace these values with your actual secrets
  # In production, use sealed-secrets, external-secrets, or vault
  JWT_SECRET: "your-super-secret-key-that-is-at-least-32-characters-long"
  JWT_ISSUER: "AnalyzerCore"
  JWT_AUDIENCE: "AnalyzerCore"
  # RPC endpoint (if using authenticated endpoints)
  # RPC_AUTH_TOKEN: "your-rpc-auth-token"
