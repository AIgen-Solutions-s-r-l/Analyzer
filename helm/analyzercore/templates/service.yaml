apiVersion: v1
kind: Service
metadata:
  name: {{ include "analyzercore.fullname" . }}
  labels:
    {{- include "analyzercore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "analyzercore.selectorLabels" . | nindent 4 }}
